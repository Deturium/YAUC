{
  "name": "yauc",
  "version": "0.1.0",
  "description": "Yet Another UBB for CC98.",
  "keywords": [
    "UBB",
    "CC98"
  ],
  "author": "Hydrogen",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean && lerna clean",
    "build": "lerna run build",
    "build:watch": "lerna run --parallel build:watch",
    "test": "lerna run --parallel test",
    "docz:dev": "lerna run --parallel --scope @cc98/ubb-site docz:dev",
    "docz:build": "lerna run --parallel --scope @cc98/ubb-site docz:build"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
