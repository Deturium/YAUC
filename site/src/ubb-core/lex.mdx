---
name: Lex
menu: UBB-CORE
---

import { Playground } from 'docz'
import { lex } from 'ubb-core'

# Lex

<Playground>
  {() => {
    const iter = lex("[b] hello world [/b]")
    return [...iter].map(i => i.rawText).join(',')
  }}
</Playground>
